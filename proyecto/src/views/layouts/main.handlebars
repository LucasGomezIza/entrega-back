<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Lista de Productos</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{title}}</h1>
            <p>Catálogo</p>
        </div>

        {{#if products}}
            <div class="products-grid">
                {{#each products}}
                <div class="product-card">
                    <div class="product-code">Código: {{this.code}}</div>
                    <h3 class="product-title">{{this.title}}</h3>
                    <p class="product-description">{{this.description}}</p>
                    
                    <div class="product-details">
                        <span class="product-price">${{this.price}}</span>
                        <span class="product-stock {{#if (gt this.stock 10)}}in-stock{{else}}{{#if (gt this.stock 0)}}low-stock{{else}}out-of-stock{{/if}}{{/if}}">
                            {{#if (gt this.stock 0)}}
                                Stock: {{this.stock}}
                            {{else}}
                                Sin stock
                            {{/if}}
                        </span>
                    </div>
                </div>
                {{/each}}
            </div>
        {{else}}
            <div class="no-products">
                <h2>No hay productos disponibles</h2>
                <p>Aún no se han agregado productos al catálogo.</p>
            </div>
        {{/if}}
    </div>
</body>
</html>

